// This module is normally built as part of the root copilot-chat-exporter
// multi-project build (see ../settings.gradle.kts). This file remains so the
// module can still be opened/built standalone if needed.
rootProject.name = "copilot-chat-reader"
